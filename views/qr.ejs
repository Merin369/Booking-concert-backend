<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            font-family: 'Poppins', sans-serif;
        }
        .container {
            margin-top: 50px;
            max-width: 600px;
            background: rgba(255, 255, 255, 0.1);
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
        }
        .qr-container {
            text-align: center;
        }
        .btn {
            width: 100%;
            font-size: 18px;
            margin-top: 15px;
        }
    </style>
</head>
<body>

<div class="container text-center">
    <h2 class="mb-3">🎟️ Booking Confirmation 🎟️</h2>
    
    <p><strong>Concert:</strong> <%= concert.name %></p>
    <p><strong>Date:</strong> <%= concert.date.toDateString() %></p>
    <p><strong>Venue:</strong> <%= concert.venue %></p>
    <p><strong>Time:</strong> <%= concert.time %></p>
    <p><strong>Tickets:</strong> <%= ticketQuantity %></p>
    <p><strong>Total Price:</strong> $<%= concert.price * ticketQuantity %></p>

    <div class="qr-container">
        <h4>Scan this QR code at the venue</h4>
        <img src="<%= qrCode %>" alt="QR Code" class="img-fluid" width="200">
    </div>

    <!-- Download PDF Button -->
    <form action="/concerts/qr/pdf/<%= concert._id %>/<%= ticketQuantity %>" method="POST">
        <button type="submit" class="btn btn-primary">Download Ticket (PDF)</button>
    </form>

    <a href="/concerts/bookings" class="btn btn-light mt-3">Back to Booking Page</a>

</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>

</body>
</html>
